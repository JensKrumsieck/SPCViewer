<Window x:Class="SPCViewer.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:converter="clr-namespace:SPCViewer.WPF.Converter"
        xmlns:extension="clr-namespace:SPCViewer.WPF.Extension"
        x:Name="mainWindow"
        mc:Ignorable="d"
        AllowDrop="True" Height="550" Width="1000" Title="{Binding Path=SelectedItem.Title, FallbackValue=SPCViewer, StringFormat={}{0} - SPCViewer}">
    <Window.Resources>
        <ResourceDictionary Source="Styles/TabTemplate.xaml" />
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding Path=ToolBoxWidth, ElementName=mainWindow, Mode=TwoWay}" MaxWidth="120" MinWidth="37"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ToolBarTray Grid.Column="0" Grid.ColumnSpan="3" Style="{DynamicResource MaterialDesignToolBarTray}" Background="{StaticResource Background}">
            <ToolBar Style="{DynamicResource ToolbarStyle}">
                <Button Style="{DynamicResource ToolbarButton}" Click="Open_Click" extension:ControlExtensions.Icon="Folder" Content="Open" extension:ControlExtensions.Orientation="Vertical"
                        HorizontalContentAlignment="Center" HorizontalAlignment="Center" Width="80"/>
                <Button Style="{DynamicResource ToolbarButton}" Click="Save_Click" extension:ControlExtensions.Icon="ContentSave" Content="Save" extension:ControlExtensions.Orientation="Vertical"
                        IsEnabled="{Binding Path=SelectedItem, Converter={converter:NotNullConverter}}" HorizontalContentAlignment="Center" HorizontalAlignment="Center"
                        Width="80"/>
            </ToolBar>
        </ToolBarTray>
        <GridSplitter Grid.Row="1" Grid.Column="1" Width="3" Background="{StaticResource Background}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        <ToolBarTray Grid.Column="0" Grid.Row="1" DockPanel.Dock="Left" Style="{DynamicResource MaterialDesignToolBarTray}" Background="{StaticResource Background}" Orientation="Vertical">
            <ToolBar Style="{DynamicResource ToolbarStyle}" Background="{StaticResource Background}" VerticalAlignment="Bottom" 
                     extension:ControlExtensions.Collapsed="{Binding Path=ToolBoxWidth, ElementName=mainWindow, Converter={converter:GridLengthToBooleanConverter}, ConverterParameter=110}">
                <ListBox SelectedIndex="{Binding SelectedItem.MouseAction, Mode=TwoWay, IsAsync=True, Converter={converter:UIActionToIntConverter}}" 
                         Background="Transparent" SelectionMode="Single" IsEnabled="{Binding Path=SelectedItem, Converter={converter:NotNullConverter}}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate><StackPanel/></ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBoxItem Style="{StaticResource ToolbarListItem}" extension:ControlExtensions.Icon="MagnifyScan" Content="Zoom"/>
                    <ListBoxItem Style="{StaticResource ToolbarListItem}" extension:ControlExtensions.Icon="EyeDropperVariant" Content="Tracker"/>
                    <ListBoxItem Style="{StaticResource ToolbarListItem}" extension:ControlExtensions.Icon="FitToPageOutline" Content="Normalize"/>
                    <ListBoxItem Style="{StaticResource ToolbarListItem}" extension:ControlExtensions.Icon="BoxCutter" Content="Peak Picking"/>
                    <ListBoxItem Style="{StaticResource ToolbarListItem}" extension:ControlExtensions.Icon="MathIntegral" Content="Integrate"/>
                    <ListBoxItem Style="{StaticResource ToolbarListItem}" extension:ControlExtensions.Icon="EyedropperPlus" Content="PickValue"/>
                </ListBox>
                <Separator Style="{StaticResource SeparatorStyle}"/>
                <ListBox SelectionMode="Extended" SelectionChanged="AdditionalSeries_VisibleChanged"  Background="Transparent" 
                         IsEnabled="{Binding Path=SelectedItem, Converter={converter:NotNullConverter}}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBoxItem Style="{StaticResource ToolbarListItem}" extension:ControlExtensions.Icon="Delta" Content="Show Derivative"
                                 IsSelected="{Binding SelectedItem.DerivSeries.IsVisible, Mode=TwoWay}"/>
                    <ListBoxItem Style="{StaticResource ToolbarListItem}" extension:ControlExtensions.Icon="MathIntegralBox" Content="Show Integral"
                                 IsSelected="{Binding SelectedItem.IntegralSeries.IsVisible, Mode=TwoWay}"/>
                </ListBox>
                <Separator Style="{StaticResource SeparatorStyle}"/>
            </ToolBar>
        </ToolBarTray>
        <TabControl x:Name="TabContainer" ContentTemplate="{StaticResource TabTemplate}" 
                    Grid.Column="2" Grid.Row="1"
                    ItemsSource="{Binding Path=Items}" Drop="OnFileDrop" 
                    SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem">
                    <Setter Property="Header" Value="{Binding Title}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type TabItem}">
                                <Border BorderThickness="0" BorderBrush="Transparent">
                                    <Border x:Name="PART_BORDER" Padding="40 10" IsHitTestVisible="True" Background="Transparent"
                                            BorderThickness="0" BorderBrush="{StaticResource ActiveColor}"
                                            materialDesign:RippleAssist.Feedback="{DynamicResource MaterialDesignFlatButtonRipple}">
                                        <Grid MinHeight="20">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="16"/>
                                                <ColumnDefinition Width="1*"/>
                                                <ColumnDefinition Width="auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Image VerticalAlignment="Center" Source="/SPCViewer.WPF;component/Resources/spc_icon.ico"/>
                                            <Label Grid.Column="1" Content="{TemplateBinding Header}" 
                                                   Foreground="{StaticResource Background}"
                                                   FontSize="14" HorizontalAlignment="Center"
                                                   FontWeight="DemiBold"
                                                   x:Name="PART_TEXT"/>
                                            <Button Grid.Column="2" Width="16" Height="16" 
                                                    Margin="2 0 0 0" VerticalAlignment="Center"
                                                    CommandParameter="{Binding}"
                                                    Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}, Path=DataContext.DeleteItemCommand}">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="{x:Type Button}">
                                                        <Grid >
                                                            <Ellipse x:Name="Bg" Fill="Transparent" Stroke="{StaticResource ActiveColor}" 
                                                                     StrokeThickness="0" />
                                                            <materialDesign:PackIcon Kind="Close" Width="12" Height="12"
                                                                HorizontalAlignment="Center"
                                                                VerticalAlignment="Center" />
                                                        </Grid>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="Bg" Property="StrokeThickness" Value="1" />
                                                                <Setter TargetName="Bg" Property="Fill" Value="#FFA6A6A6"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                            </Grid>
                                    </Border>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="PART_BORDER" Property="BorderThickness" Value="0 0 0 1.5"/>
                                        <Setter TargetName="PART_TEXT" Property="Foreground" Value="{StaticResource ActiveColor}"/> 
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="PART_BORDER" Property="Background" Value="{StaticResource HoverColorOpacity}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.ItemContainerStyle>
        </TabControl>
    </Grid>
</Window>
